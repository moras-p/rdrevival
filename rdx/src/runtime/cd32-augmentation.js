/* Auto-generated by tools/rdx/generate_cd32_augmentation.py. */
export function cd32AugmentedRdxTick(pn, tick) {
  return Number(pn) === 107 || Number(pn) === 108
    ? Math.floor(Math.max(0, Number(tick) || 0) * 6 / 5)
    : Math.max(0, Number(tick) || 0);
}

export function cd32AugmentedRdxFrame(frame, pn) {
  if (!frame || (Number(pn) !== 107 && Number(pn) !== 108)) return frame;
  return Object.freeze({ ...frame,
    cd32Augmentation:Object.freeze({ source:Number(pn) === 107 ? 'A096' : 'A097', policy:'five-tick-cadence-with-decoder-normalized-classic-alignment', presentationBobY:0 }) });
}
